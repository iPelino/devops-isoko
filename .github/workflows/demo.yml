name: Printing My Name

on: 
  pull_request:
    branches: ["main"]
  push:
    branches: ["dev"]
  workflow_dispatch: 

jobs:
  printing:
    runs-on: ubuntu-latest
    steps:
      - name: print my name
        run: echo "some name"
